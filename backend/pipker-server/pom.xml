<?xml version="1.0" encoding="UTF-8"?>
<!--
    文件：pom.xml
    项目：Pipker Framework
    模块：Pipker Server
    说明：构建可执行 HTTP Server，并组装 Pipker 独立业务模块与技术 Starter。
    处理逻辑：继承父工程默认配置，将业务 API 与 Redis、日志、Sa-Token 认证和安全加密 Starter 组合，最后通过 Spring Boot 打包应用。
    依赖：pipker-framework 父工程、pipker-business-api、Pipker Redis Starter、Pipker Log Starter、Pipker Sa-Token Starter、Pipker Security Starter、Spring Boot Web、Spring Boot Validation
    检索关键词：maven、模块、server、spring-boot
    作者：holic512
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.pipker</groupId>
        <artifactId>pipker-framework</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>pipker-server</artifactId>
    <name>Pipker Server</name>
    <description>HTTP server module for Pipker Framework.</description>

    <dependencies>
        <dependency>
            <groupId>com.pipker</groupId>
            <artifactId>pipker-business-api</artifactId>
        </dependency>
        <dependency>
            <groupId>com.pipker</groupId>
            <artifactId>pipker-spring-boot-starter-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>com.pipker</groupId>
            <artifactId>pipker-spring-boot-starter-log</artifactId>
        </dependency>
        <dependency>
            <groupId>com.pipker</groupId>
            <artifactId>pipker-spring-boot-starter-satoken</artifactId>
        </dependency>
        <dependency>
            <groupId>com.pipker</groupId>
            <artifactId>pipker-spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-liquibase</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
